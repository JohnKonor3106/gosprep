# Study Space

Веб-приложение для подготовки к государственным экзаменам. Вопросы и ответы по дисциплинам, современный интерфейс, офлайн-режим и поддержка админ-панели.



## 🚀 Stack

- **React 19**, **Vite 7** — современный фронтенд
- **Chakra UI v3** — UI-компоненты
- **Zustand** — state-менеджмент
- **React Router** — маршрутизация
- **PocketBase** — база данных и авторизация (с fallback на статические данные)

## Быстрый старт

```bash
git clone https://github.com/your-username/study-space.git
cd study-space
npm install
npm run dev
```

## Сборка и предпросмотр

```bash
npm run build     # продакшн-сборка
npm run preview   # предпросмотр билда
```

## Переменные окружения

Создайте `.env` на основе `.env.example`:

```
VITE_POCKETBASE_URL=http://127.0.0.1:8090
```

> ⚠️ Можно использовать без PocketBase — тогда берутся данные из `src/data/`.

## Скрипты

- `npm run dev` — запуск dev-сервера
- `npm run build` — production-сборка
- `npm run preview` — предпросмотр
- `npm run lint` / `lint:fix` — проверка и автоисправление
- `npm run format` — Prettier форматирование
- `npm run test` — тесты

## Структура проекта

<details>
<summary>src/</summary>

```
src/
├── admin/        # Админ-панель (PocketBase)
├── components/   # React-компоненты
├── config/       # Конфиг дисциплин и глав
├── constants/    # Константы
├── data/         # Статические вопросы/ответы
├── pages/        # Страницы приложения
├── routes/       # Маршруты
├── services/     # Внешние сервисы
├── state/        # Zustand stores
└── utils/        # Утилиты
```
</details>

## Возможности

- 🔌 **Офлайн:** работает без сервера, берет data из src/data/
- 💡 **Graceful fallback**: автоматический переход на статичные данные при ошибках сервера
- ⚡ **Code splitting**: lazy load для компонентов и маршрутов
- 📱 **Responsive дизайн** для любых устройств
- 🔒 **Безопасная админка:** авторизация через PocketBase

## Безопасность

- Только переменные с `VITE_` доступны в браузере
- Никогда не храните приватные секреты в .env с этим префиксом


