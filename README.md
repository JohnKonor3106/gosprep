# Study Space - Подготовка к госэкзаменам

Веб-приложение для подготовки к государственным экзаменам с вопросами и ответами по различным дисциплинам.

## 🚀 Технологии

- **React 19** + **Vite 7** - современный стек для фронтенда
- **Chakra UI v3** - компонентная библиотека
- **Zustand** - управление состоянием
- **React Router** - маршрутизация
- **PocketBase** - база данных (опционально, есть fallback на статические данные)

## 📦 Установка и запуск

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для продакшена
npm run build

# Просмотр собранной версии
npm run preview
```

### Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```env
VITE_POCKETBASE_URL=http://127.0.0.1:8090
```

**Важно:** Приложение может работать **без сервера PocketBase** - в этом случае используются статические данные из `src/data/`.

## 🌐 Деплой

### Деплой на Vercel

Проект готов к деплою на Vercel:

1. **Подключите репозиторий к Vercel:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Импортируйте ваш Git-репозиторий
   - Vercel автоматически определит настройки из `vercel.json`

2. **Настройте переменные окружения (опционально):**
   - В настройках проекта Vercel → Environment Variables
   - Добавьте `VITE_POCKETBASE_URL` с URL вашего PocketBase сервера
   - Если сервера нет, переменную можно не добавлять - приложение будет работать в офлайн-режиме

3. **Деплой:**
   - Vercel автоматически соберет и задеплоит проект при каждом push в main
   - Или нажмите "Deploy" вручную

### Работа без сервера (офлайн режим)

Приложение полностью готово к работе без бэкенда:

- ✅ Все данные доступны из статических файлов в `src/data/`
- ✅ При ошибке подключения к PocketBase автоматически используется fallback
- ✅ Все функции работают: просмотр вопросов, ответов, тренажер
- ⚠️ Админ-панель требует подключения к PocketBase

### Другие платформы

Проект можно задеплоить на любую платформу, поддерживающую статические сайты:

- **Netlify** - аналогично Vercel
- **GitHub Pages** - через GitHub Actions
- **Cloudflare Pages** - статический хостинг
- Любой другой статический хостинг

## 📁 Структура проекта

```
src/
├── admin/          # Админ-панель (требует PocketBase)
├── components/     # React компоненты
├── config/         # Конфигурация дисциплин и глав
├── constants/      # Константы приложения
├── data/           # Статические данные (вопросы, ответы)
├── pages/          # Страницы приложения
├── routes/         # Маршрутизация
├── services/       # Сервисы (PocketBase)
├── state/          # Zustand stores
└── utils/          # Утилиты
```

## 🛠️ Скрипты

- `npm run dev` - запуск dev-сервера
- `npm run build` - сборка для продакшена
- `npm run preview` - предпросмотр собранной версии
- `npm run lint` - проверка кода линтером
- `npm run lint:fix` - автоисправление ошибок линтера
- `npm run format` - форматирование кода
- `npm run test` - запуск тестов
- `npm run test:run` - запуск тестов один раз
- `npm run test:coverage` - тесты с покрытием

## 📝 Особенности

- **Офлайн-режим:** Приложение работает без сервера, используя статические данные
- **Graceful degradation:** При ошибке подключения к PocketBase автоматически переключается на статические данные
- **Code splitting:** Оптимизированная сборка с разделением на чанки
- **Responsive design:** Адаптивный интерфейс для всех устройств

## 🔒 Безопасность

- Переменные окружения с префиксом `VITE_` доступны в браузере (публичные)
- Приватные данные не должны иметь префикс `VITE_`
- Админ-панель защищена авторизацией через PocketBase

## 📚 Документация

Подробная документация находится в папке `docs/`:
- `ARCHITECTURE.md` - архитектура приложения
- `DEVELOPER_CHEATSHEET.md` - шпаргалка для разработчиков
- `FILES_REFERENCE.md` - справочник файлов
- `ADMIN_PANEL.md` - документация админ-панели

## ✅ Готовность к деплою

Проект полностью готов к деплою:

- ✅ Сборка проходит без ошибок
- ✅ Нет ошибок линтера
- ✅ Конфигурация Vercel настроена
- ✅ Обработка ошибок подключения к серверу
- ✅ Fallback на статические данные
- ✅ Работает без сервера

## 📄 Лицензия

Private project
